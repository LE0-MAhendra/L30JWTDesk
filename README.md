# L30JWTDesk

Local-first JWT inspection desktop frontend built with Tauri 2, React, TypeScript, Vite and Tailwind CSS.

```bash
npm install
npm run dev
```

The frontend currently uses typed fixtures in `src/services.ts`. Rust backend command files are intentionally empty for implementation by the project owner.
